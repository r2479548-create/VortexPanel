import sqlite3

db_path = r'e:\VortexPanel-main\VortexPanel-main\billing.db' # Or wherever it is
# Wait, the app config says sqlite:////opt/errormodz/billing.db which is absolute.
# Since I am editing locally and pushing to github, the database is generated ON the VPS.
